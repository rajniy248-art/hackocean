{
  "name": "hackocean",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "cd website && npm run dev",
    "build": "cd website && npm run build",
    "preview": "cd website && npm run preview"
  }
}
